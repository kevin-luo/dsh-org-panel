# 赛博公司 v1.4 设计验收

## 验收对象

- 设计参考：`C:/Users/Administrator/.codex/attachments/715b1a5b-0de2-4d78-92c3-b99a8c0be780/image-2.png`
- 原始问题界面：同附件目录 `image-1.png`
- 实现页面：DeepSeek Harness 当前会话的「赛博公司」标签页
- 对比产物：`docs/qa/reference-vs-v1.4.png`

## 最终截图

| 视口 | 产物 | 布局结果 |
| --- | --- | --- |
| 1920×1080 | `docs/qa/cyber-company-v1.4-1920x1080.png` | 通讯录、办公室、工作群、经营栏完整显示 |
| 1440×900 | `docs/qa/cyber-company-v1.4-1440x900.png` | 三栏紧凑显示，无重叠、无页面横向溢出 |
| 1280×800 | `docs/qa/cyber-company-v1.4-1280x800.png` | 工作群与办公室保持主区，经营栏切换为右侧抽屉 |
| 900×760 | 浏览器交互验收 | 通讯录和经营栏均切换为左右抽屉，页面宽度等于视口宽度 |

## 视觉核对

- 保留参考图的深色赛博办公中枢、霓虹蓝紫配色、部门分区、员工状态、任务面板和协作消息流。
- 将视觉参考适配到 DSH 宿主：不复制参考产品的虚构数据，所有员工、任务、对话、会议和交付均来自当前会话。
- 中央区域同时容纳办公室和聊天，不再出现聊天框与 DSH 原生输入框重复的问题。
- 员工使用独立头像、立绘与工位状态，不再是同一角色换名字。
- 1280px 下移除被裁切的固定三栏，经营信息改为可呼出的右侧抽屉。
- 参考与实现并排检查后，未发现办公室、通讯录、消息输入框或状态栏互相覆盖。

## 资源与运行时

- 原故障：插件尝试访问 `/plugins/dsh-org-panel/assets/...`，DSH 未暴露该静态目录，品牌图和角色资源返回 404。
- 固定处理：构建阶段把设计源图压缩为 47 个 WebP，并生成内联 data URL 资源模块；运行时不再猜测静态资产根路径。
- 浏览器实测：51 张当前可见图片，损坏图片 0；原生 DSH composer 为 `display: none`，只保留赛博公司工作群输入框。
- 包体门禁：客户端构建产物小于 3.5 MiB，`npm pack` 小于 4.5 MiB。

## 交互验收

- `@` 输入会出现秘书、老王、小刘等真实员工候选。
- `@小刘` 直连后由小刘独立子代理回复“赛博公司界面链路验收通过”。
- 当前真实消息流包含 29 条消息、1 次会议开始、1 次形成结论和 7 条可展开的工作轨迹摘要。
- 会议中老王与小刘分别发言并形成共同结论，未由秘书代写成同一个角色。
- 成长、技能、插件标签可切换；1280px 经营抽屉打开后位于视口内，900px 员工与经营双抽屉均可开合。
- 输入框 Enter 发送、Shift+Enter 换行；未发送的验证输入已清空。

## 验证命令

```text
npm run typecheck
npm run build
npm run size-check
npm pack --dry-run
```

Result: Passed
